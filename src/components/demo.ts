// This component does nothing and is here only to make sure storybook has something to
// actually render/test.

export class MyLabel extends HTMLElement {}

customElements.define('my-label', MyLabel);
